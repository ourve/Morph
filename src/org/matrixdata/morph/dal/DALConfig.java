package org.matrixdata.morph.dal;

import org.apache.log4j.Logger;

public class DALConfig {

    static Logger logger = Logger.getLogger(DALConfig.class);

    public static void init() {
        logger.info("start init DALConfig");

        //todo

        logger.info("finish init DALConfig");
    }
}
